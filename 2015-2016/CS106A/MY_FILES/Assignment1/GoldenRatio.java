/*φ = (1 + √ 5) / 2*/

import acm.program.*;

public class GoldenRatio extends ConsoleProgram{
	
	public void run(){
		double golden_ratio = (1 + Math.sqrt(5)) / 2;
		println("PHI= " + golden_ratio);
	}

}
